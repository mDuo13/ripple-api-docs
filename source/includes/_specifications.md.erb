# Transaction Specifications

A *transaction specification* specifies what a transaction should do. Each [Transaction Type](#transaction-types) has its own type of specification.

## Payment

See [Transcation Types](#transaction-types) for a description.

<%= render_table("specifications/payment.json") %>

## Trustline

See [Transcation Types](#transaction-types) for a description.

<%= render_table("specifications/trustline.json") %>

## Order

See [Transcation Types](#transaction-types) for a description.

<%= render_table("specifications/order.json") %>

## Order Cancellation

See [Transcation Types](#transaction-types) for a description.

<%= render_table("specifications/order-cancellation.json") %>

## Settings

See [Transcation Types](#transaction-types) for a description.

<%= render_table("output/get-settings.json") %>

## Suspended Payment Creation

See [Transcation Types](#transaction-types) for a description.

<%= render_table("specifications/suspended-payment-creation.json") %>

## Suspended Payment Cancellation

See [Transcation Types](#transaction-types) for a description.

<%= render_table("specifications/suspended-payment-cancellation.json") %>

## Suspended Payment Execution

See [Transcation Types](#transaction-types) for a description.

<%= render_table("specifications/suspended-payment-execution.json") %>

